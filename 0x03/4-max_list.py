#!/bin/usr/python3
"""a function named findmax(numbers) that
Takes a list of numbers as input
Returns the largest number in the list"""

def find_max(numbers):
    return max(numbers)

result = find_max([10, 25, 5, 30, 15])
print(result)